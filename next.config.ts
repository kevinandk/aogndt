import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // When the domain is ready, add `output: "export"` for a static drop
  // onto Vercel, Netlify, Cloudflare Pages, or any static host.
};

export default nextConfig;
